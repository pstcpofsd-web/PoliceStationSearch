Police Station Search Python Windows App

Run: pip install -r requirements.txt && python app.py
Build EXE: run build.bat
The app uses the same Google Apps Script endpoint from the uploaded HTML. Add and Correction are sent as Pending; only approved records returned by the API are searchable.
